[project]
name = "drone-sim-dev"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "drone-sim-artifacts",
    "drone-sim-companion",
    "drone-sim-electromagnet",
    "drone-sim-gazebo",
    "drone-sim-orchestration",
    "jsonschema>=4.23,<5",
]

[dependency-groups]
dev = [
    "pytest>=8.3,<9",
]

[tool.pytest.ini_options]
addopts = ["--import-mode=importlib"]
pythonpath = [
    "orchestration/src",
    "artifacts/src",
    "companion/src",
    "electromagnet/src",
    "gazebo/src",
]

[tool.uv.workspace]
members = ["artifacts", "companion", "electromagnet", "gazebo", "orchestration"]

[tool.uv.sources]
drone-sim-artifacts = { workspace = true }
drone-sim-companion = { workspace = true }
drone-sim-electromagnet = { workspace = true }
drone-sim-gazebo = { workspace = true }
drone-sim-orchestration = { workspace = true }
